{
  "name": "Lean seminar (Lean 4.33.0 / Mathlib v4.33.0)",

  "build": {
    "dockerfile": "Dockerfile"
  },

  "onCreateCommand": "bash -lc 'echo \"Fetching the compiled Mathlib (about 7 GB, once).\"; lake exe cache get || echo \"WARNING: the Mathlib cache did not download. Open a terminal and run: lake exe cache get\"; echo \"Setup finished. Open a file under Seminar/ in the Explorer on the left.\"'",

  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb"
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "leanprover.lean4"
      ]
    }
  }
}
